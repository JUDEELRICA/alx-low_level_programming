#include "main.h"
/**
 * _isdigit - writes a function that checks for a digit 0 to 9.
 * @c: is the int that will be used.
 * Return: 0
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}

